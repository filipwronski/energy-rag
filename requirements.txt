# PyTorch CPU-only (przed EasyOCR aby wymusić wersję bez CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
torchvision==0.25.0+cpu

pymupdf>=1.23.0
easyocr>=1.7.0
Pillow>=10.0.0
numpy>=1.24.0

# RAG system dependencies
qdrant-client>=1.7.0

# API and utilities
requests>=2.31.0
python-dotenv>=1.0.0

# RAG improvements (January 2026)
sentence-transformers==2.3.1  # Cross-encoder reranking

# Optional: Lemmatization (not fully configured)
# spacy==3.7.2

# Testing
pytest>=7.4.0
